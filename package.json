{
  "name": "trade-journal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first swing trading journal. R-denominated, event-sourced, zero dependencies.",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "start": "python3 -m http.server 8080"
  }
}
